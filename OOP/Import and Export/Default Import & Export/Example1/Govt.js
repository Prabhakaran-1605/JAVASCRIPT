// default Import & Export - If we want to export only one value then we use default Export & Import

var tax = 18;
module.exports = tax;              // ES5 

//export default tax               // ES6